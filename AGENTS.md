# 維護這個網站

這是洪聖崴選定的 C 版網站，沿用 Ken Chui 授權的原始版型。主要讀者是研究所推甄教授。

- 日常內容修改集中在 `content/site.json` 和 `content/projects/*.json`；圖片、證書放 `public/assets/`。
- 保留三項競賽：檜山坊、TapAble／BUILDMODE、保包巴士。除非使用者要求，不另起新版專案或替換整個設計。
- 區分團隊成果與洪聖崴的個人分工；不能搬用學長的隊長、開發比例、其他競賽或職歷。
- 科技法律學程維持「修習中」，除非本人提供完成資訊。
- 缺少的資料不編造；Email、照片依現有 null／預留機制處理。
- 不加入 MASTER、選校評估、私人研究文件、內部備註、離職討論或待補清單。
- 文字一律用 UTF-8。勿經 Windows PowerShell 預設文字管線寫入中文，以免變成問號。
- 修改後執行 `npm run build`，驗證內容與 7 個頁面。這不等於完成實際瀏覽器的桌機、手機或列印檢查；報告須說明檢查範圍。
- GitHub 原始碼上傳與網站公開上線分開處理。現有部署工作流程僅供手動執行，使用者確認公開上線前保留未部署狀態。
- `node_modules/`、`dist/`、`.qa/` 為產生檔，不提交。保持 `package-lock.json` 與來源同步。
